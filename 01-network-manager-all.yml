network:
  version: 2
  renderer: NetworkManager
  ethernets:
    enp0s3:
      dhcp4: no
      addresses:
        - xxx.xxx.xxx.xxx/24
      gateway4: xxx.xxx.xxx.254
      nameserver:
        addresses: [8.8.8.8]

# $ sudo apply netplan
